{
	"name": "@narrative.io/app-bridge-example-embedded-app",
	"version": "0.0.0",
	"private": true,
	"description": "Reference embedded app built from @narrative.io/app-bridge/guest and fetch, and nothing else",
	"type": "module",
	"scripts": {
		"dev": "bun run serve.ts",
		"build": "bun build src/main.ts --target=browser --outdir=dist --entry-naming=main.js && cp index.html styles.css dist/",
		"test": "bun test",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@narrative.io/app-bridge": "file:../.."
	},
	"devDependencies": {
		"@types/bun": "1.3.2"
	}
}
